rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }

    match /portfolios/{portfolioId} {
      // Allow anyone to read a portfolio by its ID
      allow get: if true;
      
      // Allow anyone to create or update a portfolio snapshot
      allow create, update: if request.resource.data.compressedData is string
                   && request.resource.data.compressedData.size() < 1048576;
      
      // Do not allow listing all portfolios
      allow list: if false;
      
      // Do not allow deleting
      allow delete: if false;
    }
  }
}
